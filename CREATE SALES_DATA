CREATE TABLE SALES_DATA (
ORDERID INTEGER,
PRODUCTID INTEGER,
CUSTOMERID INTEGER,
ORDERDATE VARCHAR(10),
UNITPRICE INTEGER,
QUANTITY INTEGER,
DISCOUNT INTEGER,
UNITSALES INTEGER,
NETSALES DECIMAL(10,2),
UNITCOST INTEGER,
COST INTEGER
) TYPE DATASOURCE STORE IN MEMORY
